{
  "name": "ats3-lsp-server",
  "version": "0.1.0",
  "private": true,
  "description": "ATS3-built LSP server (WS-1b). Standalone node script (xats-lsp-server.js) transpiled from ATS3. Owns the JSON-RPC connection, TextDocument sync, debounce, per-(uri,version) cache, and spawns the one-shot checker to publish diagnostics.",
  "license": "MIT",
  "main": "xats-lsp-server.js",
  "scripts": {
    "build": "bash build.sh",
    "smoke": "node scripts/smoke.js"
  },
  "dependencies": {
    "vscode-languageserver": "9.0.1",
    "vscode-languageserver-textdocument": "1.0.12"
  }
}
