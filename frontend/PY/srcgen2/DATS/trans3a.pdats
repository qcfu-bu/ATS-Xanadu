include "srcgen2/HATS/xatsopt_sats.hats"

include "srcgen2/HATS/xatsopt_dpre.hats"

let xatsv_ATS_PACKNAME = "ATS3.XANADU.xatsopt-20220500"

from "srcgen2/SATS/staexp2" import *

from "srcgen2/SATS/dynexp2" import *

from "srcgen2/SATS/dynexp3" import *

from "srcgen2/SATS/trans3a" import *

@impl
def d3parsed_of_trans3a(dpar):
    let stadyn = d3parsed_get_stadyn(dpar)
    let nerror = d3parsed_get_nerror(dpar)
    let source = d3parsed_get_source(dpar)
    let t1penv = d3parsed_get_t1penv(dpar)
    let t2penv = d3parsed_get_t2penv(dpar)
    let parsed = d3parsed_get_parsed(dpar)
    let env0 = tr3aenv_make_nil()
    let parsed = trans3a_d3eclistopt(env0, parsed)
    let t3penv = tr3aenv_free_top(env0)
    d3parsed(stadyn, nerror, source, t1penv, t2penv, t3penv, parsed)

@impl[]
def list_trans3a_fnp[X0, Y0](e1, xs, fopr):
    @inst[X0] @inst[Y0] @inst[e1] list_map/e1nv(xs, e1)
where:
    type E1 = tr3aenv
    @impl[X0, Y0, E1]
    def map/e1nv/fopr(x0, e1):
        fopr(e1, x0)

@impl[]
def optn_trans3a_fnp[X0, Y0](e1, xs, fopr):
    @inst[X0] @inst[Y0] @inst[e1] optn_map/e1nv(xs, e1)
where:
    type E1 = tr3aenv
    @impl[X0, Y0, E1]
    def map/e1nv/fopr(x0, e1):
        fopr(e1, x0)

