# build artifacts (regenerated by `make`) — never commit (incl. the ~200MB bundle)
BUILD/
# reference-only copies of the prior attempt's runtime (live in CATS/CHEZ already)
runtime/_reference_xats2chez/
