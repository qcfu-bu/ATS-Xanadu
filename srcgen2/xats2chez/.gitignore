# xats2chez — generated build artifacts (NEVER commit; regenerable).
# Everything under srcgen2/BUILD/ is regenerable: per-`.dats` transpiles, the
# ~200 MB emitter bundle, sed-namespaced cached fixed libs, the i0varfst shim,
# per-test work dirs (emitted .scm), and the V8 compile cache.
srcgen2/BUILD/

# Defensive: stray bundles/transpiles outside BUILD/.
*-bundle*.js
*_dats_out0.js
*_dats_ns.js
.v8cache/

# macOS noise.
.DS_Store
